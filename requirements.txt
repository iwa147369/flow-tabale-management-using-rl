torch
numpy
gymnasium
wandb
tabulate
mininet
matplotlib>=3.4.0
colorlog
pandas>=1.3.0
seaborn>=0.11.0
# ryu and eventlet must be installed in the Ubuntu VM (Python 3.10)
# they are incompatible with Python 3.14 on the Arch host
